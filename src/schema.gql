# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type DateField {
  day: Int!
  localeString: String!
  month: Int!
  year: Int!
}

"""
An input for dates. year, month, and day inputs can be provided OR an ISO8601 (YYYY-MM-DD) date string. Either provide a string OR the year, month, day values. They cannot be provided together.
"""
input DateInput {
  day: Int
  month: Int
  string: String
  year: Int
}

type Datetime {
  date: DateField!
  locale: String!
  time: TimeField!
  timestamp: Timestamp!
}

type PrayerTimes {
  date: DateField!
  timings: [Timing!]!
}

type Query {
  test(name: String): String!
  today(date: DateInput, lat: Float!, lng: Float!, locale: String! = "en-US", madhab: Int! = 1, method: String! = "MWL", timeZone: String! = "America/Toronto", timings: [String!]! = ["fajr", "sunrise", "dhuhr", "asr", "maghrib", "isha", "midnight"]): PrayerTimes!
}

type TimeField {
  hour: Int!
  localeString: String!
  millisecond: Int!
  minute: Int!
  second: Int!
}

"""
The javascript `Date` as integer. Type represents date and time as number of milliseconds from start of UNIX epoch.
"""
scalar Timestamp

type Timing {
  datetime: Datetime!
  name: String!
}